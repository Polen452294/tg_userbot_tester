# tg-userbot-tester

MTProto userbot для тестирования/автоматизации диалога с твоим Telegram-ботом (Bot API).

## Быстрый старт

1) Создай API ID/HASH:
- https://my.telegram.org → API development tools

2) Установи зависимости:
```bash
python -m venv .venv
# Windows:
.venv\Scripts\activate
# Linux/macOS:
source .venv/bin/activate

pip install -U pip
pip install .