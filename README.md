# ИНСТРУКЦИЯ ПО ИСПОЛЬЗОВАНИЮ TELEGRAM-БОТА

------------------------------------------------------------------------

## ⚠ ВАЖНО

Бот использует отдельный Telegram-аккаунт (через **Telethon,
MTProto**),\
который выполняет запросы к парсируемому боту.

Это **НЕ Bot API аккаунт**, а обычный Telegram-пользователь.

Для работы необходимо получить:

TG_API_ID\
TG_API_HASH

------------------------------------------------------------------------

## 🔑 Как получить TG_API_ID и TG_API_HASH

1.  Перейдите на сайт:\
    https://my.telegram.org

2.  Авторизуйтесь с номера Telegram-аккаунта,\
    который будет использоваться для парсинга.

3.  Перейдите в раздел:\
    **API development tools**

4.  Создайте приложение (название любое).

5.  После создания вам будут выданы:

    -   API ID
    -   API Hash

Их нужно прописать в файле `.env`:

TG_API_ID=123456\
TG_API_HASH=xxxxxxxxxxxxxxxxxxxxxxxx

Для создания `.env` в репозитории есть файл `.env.example`.\
Вы можете скопировать его содержимое в новый файл `.env`\
и подставить реальные значения переменных.

------------------------------------------------------------------------

# 1️⃣ РУЧНОЙ РЕЖИМ

Отправьте боту сообщение в формате:

ИНН; ФИО

Пример:

2222058686; Маркова Ольга Викторовна

Бот: - отправит /inn в целевой бот - нажмёт кнопку с нужным ФИО - вернёт
телефон и email (если найдены)

------------------------------------------------------------------------

# 2️⃣ ПАКЕТНЫЙ РЕЖИМ (EXCEL)

Отправьте `.xlsx` файл с колонками:

ИНН \| ФИО

Первая строка должна содержать заголовки.

Бот: - обработает строки последовательно - вернёт файл
`output_дата_время.xlsx`

Если лимит запросов исчерпан: - вернёт `output_...xlsx` - дополнительно
вернёт `pending_...xlsx` (остаток для обработки на следующий день)

------------------------------------------------------------------------

# 📊 СТАТУСЫ В EXCEL

OK --- Контакты найдены\
NOT_FOUND --- По запросу ничего не найдено\
LIMIT --- Дневной лимит исчерпан\
FORBIDDEN --- Запрет на отправку\
FLOOD --- Ограничение Telegram\
ERROR --- Иная ошибка

------------------------------------------------------------------------

# 🚀 ЗАПУСК С ЛОКАЛЬНОГО УСТРОЙСТВА

### 1. Создать виртуальное окружение

python -m venv .venv\
source .venv/bin/activate (Linux / Mac)

Windows:\
.venv`\Scripts`{=tex}`\activate`{=tex}

### 2. Установить зависимости

pip install -r requirements.txt

### 3. Создать файл `.env`

TG_API_ID=\
TG_API_HASH=\
TG_SESSION_NAME=me\
BOT_USERNAME=@target_bot_username\
CONTROL_BOT_TOKEN=

### 4. Запустить бота

python -m userbot_tester.main

------------------------------------------------------------------------

# 🖥 ЗАПУСК НА СЕРВЕРЕ

1.  Клонировать проект:\
    git clone `<repo>`{=html}\
    cd `<repo>`{=html}

2.  Создать виртуальное окружение:\
    python3 -m venv .venv\
    source .venv/bin/activate

3.  Установить зависимости:\
    pip install -r requirements.txt

4.  Создать `.env`

5.  Запустить:\
    python -m userbot_tester.main

------------------------------------------------------------------------

# 🔄 ЗАПУСК В ФОНЕ (ДЛЯ СЕРВЕРА)

Через screen:\
screen -S userbot\
python -m userbot_tester.main

Или через nohup:\
nohup python -m userbot_tester.main &

------------------------------------------------------------------------

# ❗ ВАЖНО

-   При первом запуске Telethon запросит код подтверждения Telegram.
-   Используется отдельный Telegram-аккаунт для парсинга.
-   Бот автоматически останавливает обработку при дневном лимите.
-   Повторные запросы кэшируются для снижения нагрузки.
